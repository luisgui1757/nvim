.DS_Store
**/.DS_Store

# Test scratch space
tests/.cache/
tests/tmp/
tests/**/tmp/

# Backup files produced by the bootstrap scripts
*.bak.*

# Claude Code local state -- per-machine sessions, scheduled-task locks, etc.
# This repo no longer ships a synced claude/ folder; preferences live here only
# locally. Anything that lands in .claude/ is per-machine noise.
.claude/

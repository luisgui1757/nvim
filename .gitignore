.DS_Store
**/.DS_Store

# Test scratch space
tests/.cache/
tests/tmp/
tests/**/tmp/

# Backup files produced by the bootstrap scripts
*.bak.*

# Claude Code local state -- per CLAUDE.md, .claude/ (dotted) is local-only
# (sessions, scheduled-task locks, etc.); the synced settings live in claude/
# (no dot) at the repo root. Anything that lands here is per-machine noise.
.claude/
